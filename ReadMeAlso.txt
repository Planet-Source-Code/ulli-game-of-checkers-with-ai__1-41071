The rules to apply are controlled by #Const Rules.

When the value is 1 (one) then it compiles the International Version.
When the value is 0 (zero) then it compiles the AFC Version.

You will find the rules in Checkers.Rules.Txt.